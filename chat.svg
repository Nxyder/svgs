<svg xmlns="http://www.w3.org/2000/svg" width="100vw" height="100vh" style="position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; margin: 0; padding: 0; overflow: hidden; display: block;">
  <foreignObject x="0" y="0" width="100%" height="100%" style="width: 100vw; height: 100vh; margin: 0; padding: 0; overflow: hidden;">
    <body xmlns="http://www.w3.org/1999/xhtml" style="margin:0;padding:0;overflow:hidden;height:100vh;height:100dvh;width:100vw;background:#000;">
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
        <style>
          html, body {
            margin: 0 !important;
            padding: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            height: 100dvh !important;
            overflow: hidden !important;
            background: #000;
          }
          #loadingScreen, #mainFrame {
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            right: 0 !important;
            bottom: 0 !important;
            width: 100vw !important;
            height: 100vh !important;
            height: 100dvh !important;
            border: none !important;
            margin: 0 !important;
            padding: 0 !important;
            overflow: hidden !important;
          }
        </style>
      </head>

      <!-- Laadscherm -->
      <div id="loadingScreen" style="display:flex;align-items:center;justify-content:center;background:#000;z-index:999999;transition:opacity 0.5s ease;">
        <div style="width:200px;height:1px;background:#222;position:relative;overflow:hidden;">
          <div id="loadBar" style="position:absolute;top:0;left:0;height:100%;width:0%;background:#fff;transition:width 0.2s ease;"></div>
        </div>
      </div>

      <iframe 
        id="mainFrame"
        src="about:blank"
        style="z-index:999998;">
      </iframe>

      <script type="text/javascript">
        <![CDATA[
        (function() {
          var iframe = document.getElementById('mainFrame');
          var loadingScreen = document.getElementById('loadingScreen');
          var loadBar = document.getElementById('loadBar');

          var url = 'https://cdn.jsdelivr.net/gh/nxyder/svgs/b64-3.txt';

          loadBar.style.width = '30%';

          fetch(url)
            .then(function(response) {
              if (!response.ok) {
                throw new Error('HTTP error! status: ' + response.status);
              }
              loadBar.style.width = '70%';
              return response.text();
            })
            .then(function(base64Data) {
              var decodedHtml = atob(base64Data);

              var doc = iframe.contentDocument || iframe.contentWindow.document;
              doc.open();
              doc.write(decodedHtml);
              doc.close();

              loadBar.style.width = '100%';

              setTimeout(function() {
                loadingScreen.style.opacity = '0';
                setTimeout(function() {
                  loadingScreen.style.display = 'none';
                }, 500);
              }, 300);
            })
            .catch(function(error) {
              console.error('Fout bij ophalen:', error);
              loadingScreen.style.opacity = '0';
              setTimeout(function() {
                loadingScreen.style.display = 'none';
              }, 500);
            });
        })();
        ]]>
      </script>
    </body>
  </foreignObject>
</svg>
